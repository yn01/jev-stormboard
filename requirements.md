# jev-stormboard 要件リスト

> **注記: このファイルは Python の依存関係ファイル `requirements.txt` とは別物です。**
> 依存ライブラリは `requirements.txt`、プロジェクトの要件はこの `requirements.md` に書きます。
> 今後の開発は、この文書を起点に進めます。要件の追加や実装完了があれば、都度更新してください。

気象庁の防災情報XML(防災電文)をリアルタイムに取り込み、TypeSafe の Jev で
「この電文は自分にとって何を意味するか」を判定させるデモです。

> **⚠️ 実際の防災判断には使いません。** 避難や行動の判断は、気象庁および自治体の発表に従ってください。

状態は【実装済み】【未実装】【検討中】で示します。

---

## 1. ロガー(防災電文の収集)

| ID | 要件 | 状態 |
| --- | --- | --- |
| R-01 | 気象庁の防災情報XML Atomフィード4種(随時の高頻度・長期、定時の高頻度・長期)を取得する。地震火山フィードとその他フィードは取得しない | 【実装済み】 |
| R-02 | 随時・定時とも**全エントリ**を保存する。種類(title)による絞り込みは設定として残す(`REGULAR_TITLE_INCLUDES` / `TITLE_EXCLUDES`、初期値は絞り込みなし) | 【実装済み】 |
| R-03 | 高頻度フィードを60秒間隔でポーリングする | 【実装済み】 |
| R-04 | 起動時と1時間ごとに、長期フィードで停止中の分を穴埋めする。遡る上限は24時間(`BACKFILL_MAX_AGE_HOURS`) | 【実装済み】 |
| R-05 | 重複は entry の id で排除する。起動時に `data/index.jsonl` から保存済みIDを読み込み、再起動しても続きから保存する | 【実装済み】 |
| R-06 | フィードの取得に `If-Modified-Since` / `ETag` による条件付きGETを使う。304を受けても検証子を保持し続ける | 【実装済み】 |
| R-07 | 電文本体のダウンロードは並列度2、1件ごとに0.2秒待つ。User-Agent とタイムアウト(30秒)を設定し、気象庁のサーバーに負荷をかけない | 【実装済み】 |
| R-08 | 通信エラーやXMLのパース失敗でプロセスを落とさない。指数バックオフで最大4回リトライし、失敗した電文は索引に載らないため次の周期で再試行される | 【実装済み】 |
| R-09 | 電文本体を `data/raw/YYYY-MM-DD/<電文ファイル名>.xml.gz` に保存する。日付は entry の updated を JST に直したもの | 【実装済み】 |
| R-10 | 一時ファイルに書いてから rename する。途中で落ちても壊れたファイルを残さない | 【実装済み】 |
| R-11 | `data/index.jsonl` に1電文1行を追記する。項目は id / title / updated / author / feed / url / path / bytes / fetched_at と、content の要約文(summary) | 【実装済み】 |
| R-12 | SIGINT / SIGTERM を受けたら安全に終了する | 【実装済み】 |
| R-13 | ログを画面と `logs/logger.log` に出す(5MBごとにローテーション、5世代) | 【実装済み】 |
| R-14 | 随時フィードの新着が30分以上途切れたら WARNING を出す(`NO_NEW_ENTRY_WARN_SEC`)。再通知は30分間隔 | 【実装済み】 |
| R-15 | `python -m logger.stats` で保存件数、種類別の件数、日別の件数と容量、合計容量、最終取得時刻、フィード別の「最終の新着からの経過時間」を表示する | 【実装済み】 |
| R-16 | 設定(ポーリング間隔、フィルタ、除外リスト、保存先、しきい値)を `logger/config.py` の1か所にまとめる | 【実装済み】 |
| R-17 | パース・フィルタ・重複排除・再起動時の復元・無着信警告をテストで確認する | 【実装済み】 |

---

## 2. 判定コア(Jevによる電文の判定)

| ID | 要件 | 状態 |
| --- | --- | --- |
| R-20 | 判定対象の人物プロファイルを1件、`profile.yaml` に持つ(id / name / area_code / pref / city / lat / lon / profile の自由記述)。area_code は保存済み警報XMLの Area/Code から実在する値を設定する | 【実装済み】 |
| R-21 | 質問を `core/questions.py` に定義する。型は Jev の Choice / Score / Noul を使う。初期値は10問 | 【実装済み】 |
| R-22 | 質問はリストとして定義し、**要素を足すだけで質問数を増やせる**構造にする(10問 → 60問など) | 【実装済み】 |
| R-23 | 各質問に観点のタグ(message / plan / prepare / move / home / family など)を持たせる。将来、観点ごとの絞り込みや画面でのグループ表示に使う | 【実装済み】 |
| R-24 | 全問を**1回のJevリクエストにまとめて送る**。質問ごとに個別のリクエストを送らない | 【実装済み】 |
| R-25 | `data/index.jsonl` から電文を選び、対応するXMLを読んで Jev へ渡す state を組み立てる。state には電文の見出しと本文、発表官署、発表時刻、対象地域、プロファイルを含める | 【実装済み】 |
| R-26 | XMLの構造は電文の種類ごとに異なる。**気象警報・注意報**(VPWW53/VPWW54)と**府県気象情報**(VPFJ50)を優先して対応する。すべての種類に対応する必要はない | 【実装済み】 |
| R-27 | 結果は、質問ごとの値・確率・確信度を持つ形で返す(→ 後述の「Noul の確信度の扱い」) | 【実装済み】 |
| R-28 | `python -m core.judge --latest` で最新の電文1本を判定し、結果を表で表示する。`--id <電文ID>` で電文を指定して判定できる(同じ電文を繰り返し試すため) | 【実装済み】 |
| R-29 | APIキーは `.claude/settings.local.json` の env(`TYPESAFE_API_KEY`)から読む。コードに直接書かない | 【実装済み】 |
| R-30 | レイテンシと、リクエストの入力トークン数をログに出す。後でコスト表示に使う | 【実装済み】 |

| R-31 | 判定コアのテスト(XMLの読み取り、state の組み立て、質問の拡張、Noul の確信度の扱い) | 【実装済み】 |

動作確認の記録(2026-09-19):

- 台風第25号の気象解説情報(千葉県・山梨県)と最新電文で `python -m core.judge` が動作
- 10問を1リクエストで送って **584〜624ms**、入力トークン 3,505〜5,162、モデル `jev-1.13.0`
- `core/questions.py` に Q を1つ足すだけで11問になり、**634ms** とレイテンシはほぼ変わらなかった
  (R-61「質問数を増やしてもレイテンシが変わらない」デモの裏付けになる)

---

## 3. 今後の検討・実装対象

### 質問の拡張

| ID | 要件 | 状態 |
| --- | --- | --- |
| R-40 | 質問を10問から段階的に増やす(生活・移動・備え・住まい・周囲などの観点へ拡張。60問規模を想定) | 【未実装】 |
| R-41 | 画面上から質問を追加・削除・編集できるようにする | 【検討中】(実装するかどうか未決定) |

### 画面(Streamlit)

| ID | 要件 | 状態 |
| --- | --- | --- |
| R-50 | Streamlit の画面を作る(電文フィード、判定結果の一覧、確信度スライダー、レイテンシとコストの表示) | 【未実装】 |
| R-51 | 「実際の防災判断には使わないデモである」という注意書きを、画面に常時表示する | 【未実装】 |

### デモ機能

| ID | 要件 | 状態 |
| --- | --- | --- |
| R-60 | 保存済み電文のリプレイモード(倍速再生)。`data/replay/` を使う | 【未実装】 |
| R-61 | 質問数を切り替えて、レイテンシがほとんど変わらないことを見せるデモ機能 | 【未実装】 |
| R-62 | 通常のLLMとの処理速度・コストの比較表示 | 【未実装】 |

### 対象の拡張

| ID | 要件 | 状態 |
| --- | --- | --- |
| R-70 | プロファイルを複数に拡張する(ペルソナ40人、または拠点40件) | 【未実装】 |
| R-71 | 交通量データ(国土交通省の交通量API)の重ね合わせ | 【未実装】 |

---

## Noul の確信度の扱い

Jev の SDK が返す答えは、型によって形が違います。

| 型 | 値 | 確率 | 確信度 |
| --- | --- | --- | --- |
| Choice | `choice`(選択肢名) | `probabilities`(選択肢ごと) | `confidence` |
| Score | `score`(数値) | `probabilities`(段階ごと) | `confidence` |
| Noul | `noul`(0〜1) | 値そのものが確率 | **SDKは返さない** |

**Noul の値は「命題が真である確率」そのもの**であり、Choice / Score の `confidence` とは
意味が異なります(`confidence` は「モデルがその答えをどれだけ確信しているか」)。混同しないこと。

そのため、**画面でしきい値による絞り込みを行う際は、Noul については `0.5` からの距離
(`abs(noul - 0.5) * 2`)を確信度の代わりに用います。** 値が 0 または 1 に近いほど
「真とも偽とも判断がついている」、0.5 に近いほど「どちらとも言えない」という意味になります。

---

## 参考: 電文XMLの構造(確認済み)

保存済みの電文を読んで確認した構造です(2026-09-19 時点)。

共通部分:

- `Report/Control`: Title(電文の種類名) / DateTime / Status / EditorialOffice / PublishingOffice
- `Report/Head`: Title(見出し) / ReportDateTime(JST) / TargetDateTime / InfoType(発表・訂正・取消) /
  Serial / InfoKind / Headline/Text(要約文)

種類ごとの本文:

- **気象警報・注意報**(VPWW53 / VPWW54): `Body/Warning[@type]/Item/Area{Name, Code}` と
  `Item/Kind{Name, Status}`。type は府県予報区等 / 一次細分区域等 / 市町村等をまとめた地域等 / 市町村等の4階層
- **府県気象情報**(VPFJ50): `Body/Comment/Text` に平文

要素ごとに既定の名前空間が異なる(`jmaxml1/`、`jmaxml1/body/meteorology1/`、
`jmaxml1/informationBasis1/`)ため、パース後にタグから名前空間を落として扱います。

地域コードの階層(東京都の例):

| 区分 | 例 | コード |
| --- | --- | --- |
| 府県予報区 | 東京都 | 130000 |
| 一次細分区域 | 東京地方 | 130010 |
| 市町村等をまとめた地域 | ２３区西部 | 130011 |
| 市町村等 | 世田谷区 | 1311200 |
