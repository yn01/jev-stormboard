# 東京の防災電文、Jevで判定してみた — 要件リスト

> アプリ名: **東京の防災電文、Jevで判定してみた**（リポジトリ名は `jev-stormboard`）

> **注記: このファイルは Python の依存関係ファイル `requirements.txt` とは別物です。**
> 依存ライブラリは `requirements.txt`、プロジェクトの要件はこの `requirements.md` に書きます。
> 今後の開発は、この文書を起点に進めます。要件の追加や実装完了があれば、都度更新してください。

気象庁の防災情報XML(防災電文)をリアルタイムに取り込み、TypeSafe の Jev で
「この電文は自分にとって何を意味するか」を判定させるデモです。

> **⚠️ 実際の防災判断には使いません。** 避難や行動の判断は、気象庁および自治体の発表に従ってください。

状態は【実装済み】【未実装】【検討中】で示します。

---

## 0. 進め方(2026年9月の連休)

要件の一覧とは別に、日程と運用を時系列でまとめます。

### デモの日程

| 日付 | やること | 状態 |
| --- | --- | --- |
| 9/19 | ロガーの稼働、判定コア、Streamlit画面(リプレイ含む)まで | **完了**(当初予定より前倒し) |
| 9/20 | 表示の改善と質問数の拡張(Score表示、10/30/50問の切り替え、プロファイル反映) | **完了** |
| 9/20 | 画面の3層化(結論バナー・モード表示・リプレイ操作)と公開対応(profile.local.yaml) | **完了** |
| 9/20 | 日本地図による関連度のヒートマップ表示(R-90) | **完了** |
| 9/21 | **台風第25号の接近当日。ライブモードで稼働させ、画面を録画する** | 【本番】 |
| 9/22〜23 | マップ連動(R-91)、交通量データの重ね合わせ(R-71)、通常のLLMとの速度・コスト比較(R-62) | 未着手 |

9/19 と 9/20 に予定していた作業は、すべて完了しています。

### 9/21 本番当日の手順

- [ ] 事前に `profile.yaml` を自分の実際の状況に更新しておく
      (書き換えたら画面を再読み込みすれば反映される。前提が変わるので、
      過去の判定と混ざっている旨の警告が出る)
- [ ] ロガーが稼働していることを `python -m logger.stats` で確認する
      (**最終の新着からの経過時間**を見る。随時フィードが30分以上途切れていたら要確認)
- [ ] ライブモードで画面を起動する
      `streamlit run app/streamlit_app.py`
- [ ] 電文が増える時間帯に画面を録画する。判定が次々に更新される様子を残す
- [ ] 録画には、**指標の帯(レイテンシ・コスト)が映るようにする**
      (質問数を切り替えて、レイテンシが変わらないことも撮っておくとよい)
- [ ] 終了後、その日の判定結果が `data/judged/judgements.jsonl` に残っていることを確認する

### 運用上の注意

守るルール(data/raw への書き込み禁止、秘密情報、気象庁への負荷など)は
`CLAUDE.md` に書いてあります。ここには日程に関わるものだけを書きます。

- **ロガーは連休中ずっと稼働させる。** 止まった場合も、24時間以内に起動し直せば
  起動時の穴埋めで回収される(R-04。それより長く止めた場合は
  `BACKFILL_MAX_AGE_HOURS` を一時的に大きくする)。
- **判定結果はキャッシュされるため、リプレイを繰り返しても API コストは増えない**(R-53)。
  リハーサルは何度やってもよい。ただし質問数を変えると、その質問数で判定し直す。
- **本番当日は、実装よりも稼働と記録を優先する。大きな変更は当日に入れない。**
- **画面は必ずこのプロジェクトの `.venv` で起動する。**
  ほかのプロジェクト(jev-intent など)の `.venv/bin/streamlit` で起動すると、
  `plotly` や `httpx` が無くて `ModuleNotFoundError` になる。
  `which streamlit` で `jev-stormboard/.venv/bin/streamlit` が出ることを確認する。
  (2026-09-20 に実際に起きた。`plotly` が無い場合でも画面が落ちないようにはしてある → R-90c)

---

## 1. ロガー(防災電文の収集)

| ID | 要件 | 状態 |
| --- | --- | --- |
| R-01 | 気象庁の防災情報XML Atomフィード4種(随時の高頻度・長期、定時の高頻度・長期)を取得する。地震火山フィードとその他フィードは取得しない | 【実装済み】 |
| R-02 | 随時・定時とも**全エントリ**を保存する。種類(title)による絞り込みは設定として残す(`REGULAR_TITLE_INCLUDES` / `TITLE_EXCLUDES`、初期値は絞り込みなし) | 【実装済み】 |
| R-03 | 高頻度フィードを60秒間隔でポーリングする | 【実装済み】 |
| R-04 | 起動時と1時間ごとに、長期フィードで停止中の分を穴埋めする。遡る上限は24時間(`BACKFILL_MAX_AGE_HOURS`) | 【実装済み】 |
| R-05 | 重複は entry の id で排除する。起動時に `data/index.jsonl` から保存済みIDを読み込み、再起動しても続きから保存する | 【実装済み】 |
| R-06 | フィードの取得に `If-Modified-Since` / `ETag` による条件付きGETを使う。304を受けても検証子を保持し続ける | 【実装済み】 |
| R-07 | 電文本体のダウンロードは並列度2、1件ごとに0.2秒待つ。User-Agent とタイムアウト(30秒)を設定し、気象庁のサーバーに負荷をかけない | 【実装済み】 |
| R-08 | 通信エラーやXMLのパース失敗でプロセスを落とさない。指数バックオフで最大4回リトライし、失敗した電文は索引に載らないため次の周期で再試行される | 【実装済み】 |
| R-09 | 電文本体を `data/raw/YYYY-MM-DD/<電文ファイル名>.xml.gz` に保存する。日付は entry の updated を JST に直したもの | 【実装済み】 |
| R-10 | 一時ファイルに書いてから rename する。途中で落ちても壊れたファイルを残さない | 【実装済み】 |
| R-11 | `data/index.jsonl` に1電文1行を追記する。項目は id / title / updated / author / feed / url / path / bytes / fetched_at と、content の要約文(summary) | 【実装済み】 |
| R-12 | SIGINT / SIGTERM を受けたら安全に終了する | 【実装済み】 |
| R-13 | ログを画面と `logs/logger.log` に出す(5MBごとにローテーション、5世代) | 【実装済み】 |
| R-14 | 随時フィードの新着が30分以上途切れたら WARNING を出す(`NO_NEW_ENTRY_WARN_SEC`)。再通知は30分間隔 | 【実装済み】 |
| R-15 | `python -m logger.stats` で保存件数、種類別の件数、日別の件数と容量、合計容量、最終取得時刻、フィード別の「最終の新着からの経過時間」を表示する | 【実装済み】 |
| R-16 | 設定(ポーリング間隔、フィルタ、除外リスト、保存先、しきい値)を `logger/config.py` の1か所にまとめる | 【実装済み】 |
| R-17 | パース・フィルタ・重複排除・再起動時の復元・無着信警告をテストで確認する | 【実装済み】 |

---

## 2. 判定コア(Jevによる電文の判定)

| ID | 要件 | 状態 |
| --- | --- | --- |
| R-20 | 判定対象の人物プロファイルを1件、`profile.yaml` に持つ(id / name / area_code / pref / city / lat / lon / profile の自由記述)。area_code は保存済み警報XMLの Area/Code から実在する値を設定する | 【実装済み】 |
| R-21 | 質問を `core/questions.py` に定義する。型は Jev の Choice / Score / Noul を使う。初期値は10問 | 【実装済み】 |
| R-22 | 質問はリストとして定義し、**要素を足すだけで質問数を増やせる**構造にする(10問 → 60問など) | 【実装済み】 |
| R-23 | 各質問に観点のタグ(message / plan / prepare / move / home / family など)を持たせる。将来、観点ごとの絞り込みや画面でのグループ表示に使う | 【実装済み】 |
| R-24 | 全問を**1回のJevリクエストにまとめて送る**。質問ごとに個別のリクエストを送らない | 【実装済み】 |
| R-25 | `data/index.jsonl` から電文を選び、対応するXMLを読んで Jev へ渡す state を組み立てる。state には電文の見出しと本文、発表官署、発表時刻、対象地域、プロファイルを含める | 【実装済み】 |
| R-26 | XMLの構造は電文の種類ごとに異なる。**気象警報・注意報**(VPWW53/VPWW54)と**府県気象情報**(VPFJ50)を優先して対応する。すべての種類に対応する必要はない | 【実装済み】 |
| R-27 | 結果は、質問ごとの値・確率・確信度を持つ形で返す(→ 後述の「Noul の確信度の扱い」) | 【実装済み】 |
| R-28 | `python -m core.judge --latest` で最新の電文1本を判定し、結果を表で表示する。`--id <電文ID>` で電文を指定して判定できる(同じ電文を繰り返し試すため) | 【実装済み】 |
| R-29 | APIキーは `.claude/settings.local.json` の env(`TYPESAFE_API_KEY`)から読む。コードに直接書かない | 【実装済み】 |
| R-30 | レイテンシと、リクエストの入力トークン数をログに出す。後でコスト表示に使う | 【実装済み】 |

| R-31 | 判定コアのテスト(XMLの読み取り、state の組み立て、質問の拡張、Noul の確信度の扱い) | 【実装済み】 |

動作確認の記録(2026-09-19):

- 台風第25号の気象解説情報(千葉県・山梨県)と最新電文で `python -m core.judge` が動作
- 10問を1リクエストで送って **584〜624ms**、入力トークン 3,505〜5,162、モデル `jev-1.13.0`
- `core/questions.py` に Q を1つ足すだけで11問になり、**634ms** とレイテンシはほぼ変わらなかった
  (R-61「質問数を増やしてもレイテンシが変わらない」デモの裏付けになる)

---

## 3. 今後の検討・実装対象

### 質問の拡張

| ID | 要件 | 状態 |
| --- | --- | --- |
| R-40 | 質問を**50問**に拡張する。観点は移動(電車・車・自転車・徒歩)、備え(買い出し・充電・ベランダ・浸水対策)、住まい(停電・窓・浸水)、周囲(家族・ペット・近所)、仕事(出社・在宅)など | 【実装済み】 |
| R-41 | 画面上から質問を追加・削除・編集できるようにする | 【検討中】(実装するかどうか未決定) |

### 画面(Streamlit)

| ID | 要件 | 状態 |
| --- | --- | --- |
| R-50 | Streamlit の画面を作る(電文フィード、判定結果の一覧、確信度スライダー、レイテンシとコストの表示) | 【実装済み】 |
| R-51 | 「実際の防災判断には使わないデモである」という注意書きを、画面に常時表示する | **【一時的に非表示】**(2026-09-20、画面を見やすくするため依頼により削除。**X で公開する前に必ず戻すこと**) |
| R-55 | Score は「2.82 / 4」のように目盛りの最大値を併記し、バーの長さもその範囲に対する比率にする | 【実装済み】 |
| R-56 | `profile.yaml` を書き換えたら、画面の再読み込みで反映される(プロファイルはキャッシュしない) | 【実装済み】 |
| R-57 | 判定結果に、どのプロファイルで判定したか(id と指紋)を記録する。前提の異なる判定が混ざっている場合は画面に表示する | 【実装済み】 |
| R-58 | 判定結果のキャッシュは電文IDと**質問数**の組で持つ。質問数が違えば結果も違うため | 【実装済み】 |
| R-59 | 画面を3層に組み替える。第1層=結論バナー、第2層=補足指標、第3層=Jevの性能指標 | 【実装済み】 |
| R-59a | **結論バナー**: 最新の「注目の判定」から Choice action を1つだけ、画面幅いっぱいに大きく出す(他の3倍程度)。行動ごとに背景色を変える(通常どおり=緑 / 予定変更を検討=黄 / 今日中に備える=橙 / 外出を控える=赤 / 早めの避難を検討=濃い赤)。根拠の電文の見出しと時刻を小さく添える。該当がなければ「関係する情報はまだありません」を灰色で出す | 【実装済み】 |
| R-59b | **補足指標**: バナーの下に、切迫している(Noul)・事象の深刻さ(Score)・生活への影響度(Score)の3つだけを小さく出す | 【実装済み】 |
| R-59c | **性能指標**: 処理件数・レイテンシ・質問数・トークン・コストは画面下部の折りたたみへ移す。「状況」ではなく「Jevの性能」を示す数字のため。デモの主張として重要なので初期状態は開いておく | 【実装済み】 |
| R-59d | 動作モードを本文上部(結論バナーの上)にも常時表示する。ライブ=緑の点(点滅)と最終判定からの経過時間、リプレイ=対象日と再生速度。リプレイ中は背景の色味を変え、上下に帯を出して見分けられるようにする | 【実装済み】 |
| R-59f | リプレイ中は、**再生位置までに発表された電文だけ**を画面に出す。全件を新しい順に出すと、朝を再生していても最新の電文が先頭に出続け、再生している様子が画面に出ない | 【実装済み】 |
| R-59g | モード帯に、リプレイの再生中の時刻(⏱)を出す。モード帯は2秒ごとに描き直す | 【実装済み】 |
| R-59e | モード表示の横に操作ボタンを置く。ライブ=索引を即座に見に行く「いますぐ確認」、リプレイ=再生 / 一時停止 / 最初から。デモ中に説明しながら止められるようにするため | 【実装済み】 |

### デモ機能

| ID | 要件 | 状態 |
| --- | --- | --- |
| R-60 | 保存済み電文のリプレイモード(倍速再生)。画面のサイドバーから対象日・**開始時刻**・再生速度(**1 / 10 / 60 / 100倍**)を選ぶ | 【実装済み】 |
| R-52 | 判定はバックグラウンドのワーカーで進め、画面は `st.fragment(run_every)` で描画だけを更新する | 【実装済み】 |
| R-53 | 判定結果を `data/judged/judgements.jsonl` に保存し、再起動しても履歴が残る。同じ電文は二重に判定しない | 【実装済み】 |
| R-54 | APIのレート制限に当たったら画面に表示し、処理を止めずに間隔を空けて再試行する | 【実装済み】 |
| R-61 | サイドバーで質問数(10問 / 30問 / 50問)を切り替える。質問数別の平均レイテンシを画面に併記し、質問数を増やしてもレイテンシがほとんど変わらないことを見せる | 【実装済み】 |
| R-62 | 通常のLLMとの処理速度・コストの比較表示 | 【未実装】 |

### 公開への対応

| ID | 要件 | 状態 |
| --- | --- | --- |
| R-80 | `profile.yaml` は**公開用の仮プロファイル**として維持する。表示名は「shnch3211さん」、地域は東京都のみ(city は空)とし、特定の市区町村に寄らない内容にする | 【実装済み】 |
| R-81 | `profile.yaml` の area_code は市町村コードではなく**東京都の府県予報区コード(130000)**を使う。判定の質が保てることを確認済み(下の「公開の方針」参照) | 【実装済み】 |
| R-82 | `profile.local.yaml` を置けばそちらを優先して読む。`.gitignore` に入れ、絶対にコミットされないようにする | 【実装済み】 |
| R-83 | `profile.local.example.yaml` で書き方の例を示す(中身は仮の内容) | 【実装済み】 |
| R-84 | どちらのプロファイルを読み込んだかを画面に表示する。**画面上部には出さない**(公開用の画面を撮るときに邪魔になるため)。下の「Jev の性能」の行に出す | 【実装済み】 |
| R-85 | `data/judged/` が `.gitignore` に含まれていることを確認する(判定結果にプロファイルが記録されるため) | 【実装済み】 |
| R-86 | README に「自分の状況で試すには profile.local.yaml を作る」案内の節を追加する | 【実装済み】 |

### 地図表示

| ID | 要件 | 状態 |
| --- | --- | --- |
| R-90 | 日本地図による関連度のヒートマップ表示。**気象庁の警報マップとは別物**で、色は「警報の強さ」ではなく Jev が判定した「自分にとっての関連度」(relevant)。下敷きに電文の発表対象地域を薄いグレーで塗り、その上に relevant の最大値で色を重ねる。凡例と説明文で「警報の強さではない」ことを明示する | 【実装済み】 |
| R-90a | プロファイルの居住地に目印を置く。地図の横に関連度の高い地域の一覧を添える | 【実装済み】 |
| R-90b | 地図が重い場合に備え、サイドバーから非表示にできるようにする(初期状態は表示) | 【実装済み】 |
| R-90d | 地図は**関東圏(1都6県＋山梨)**だけを描く。全国だと見たい関東が小さくて読み取れない。東京都の伊豆諸島・小笠原は関東の地図から外す | 【実装済み】 |
| R-90e | 地図は**自前の SVG**で描く。形(パス)は一度作れば変わらず、更新のたびに変わるのは塗りの色と目印だけ。描画ライブラリを挟まないので、再描画で一瞬消えるちらつきが起きない。色の変化には CSS transition をかける | 【実装済み】 |
| R-92 | 画面をダークテーマにする(`.streamlit/config.toml`)。各要素の配色を暗い背景に合わせて調整する | 【実装済み】 |
| R-93 | 画面の再描画間隔をそろえる(現在 **1秒**)。あわせて、判定結果の並べ替えをキャッシュして毎回のソートをなくす | 【実装済み】 |
| R-95 | 関連度の色に**赤や橙を使わない**。警報の強さと見間違えられるため、青 → インディゴ → 紫 → マゼンタの系統にする | 【実装済み】 |
| R-96 | 関連度と**事象の深刻さ**を1つの地図で同時に見せる。面の色=関連度(Jevの判定)、中央の円の大きさ=深刻さ(電文から読める値)。色相を分けることで2つの軸を混同させない | 【実装済み】 |
| R-97 | 地図の横に、関東8都県の関連度と深刻さを数字でも並べる | 【実装済み】 |
| R-103 | アプリ名を日本語にする。画面は「東京の防災電文、Jevで判定してみた」、ブラウザのタブは幅が狭いので「東京の防災電文 × Jev」。**リポジトリ名 `jev-stormboard` は変えない**(URL が変わるため) | 【実装済み】 |
| R-102 | **速報欄をこの画面の主役として扱う。** 判定した行動の色で枠を描き(2px)、いちばん目を引く見た目にする。結論バナーは右の補足指標と同じ器(パネル背景＋細い枠)に落とし、文字だけを行動の色にする | 【実装済み】 |
| R-101 | 行動の表示を呼びかけ調にし(「早めの備えを」など)、副題に**警戒レベル相当**を添える。**Jev に渡す選択肢の名前は変えない**(変えると保存済みの判定と食い違い、全件の再判定が要る) | 【実装済み】 |
| R-100 | 結論バナーと地図の間に、**判定した電文を1行で流す速報欄**を置く。見出しは付けず、いちばん新しい判定だけを出す。Jev が次々に判定している様子をそのまま見せるため | 【実装済み】 |
| R-99 | リプレイに**開始時刻**のスライダー(30分刻み)を置く。対象日と再生速度の間に配置し、その時刻より前の電文は飛ばして再生する | 【実装済み】 |
| R-98 | 凡例は地図の枠の下ではなく、**横の一覧の下**に縦積みで置く。一覧には色のバーと円が並んでいるので、そのすぐ下にあると意味を突き合わせやすく、地図の描画領域も削らない | 【実装済み】 |
| R-94 | 画面上部を1画面に収める。タイトル・プロファイル・モード・操作ボタンを1行に、結論バナーと補足指標3つを横一列にして、**関連度マップがスクロールなしで見える**ようにする | 【実装済み】 |
| R-90c | `plotly` が無い環境でも画面全体を落とさない。地図の代わりに案内と、関連度の高い地域の数値だけを出す | 【実装済み】 |
| R-91 | マップ上の地域をクリックして「注目の判定」を絞り込む連動 | 【未実装】(9/22以降) |

#### R-90 の実装メモ(2026-09-20)

**描画方式: 自前の SVG(2026-09-20 に Plotly から変更)。**

当初は Plotly の `go.Choropleth` で描いていたが、更新のたびに図全体が作り直されて
**一瞬消えてからパッと出る**ちらつきが出た。地図の形は変わらず、変わるのは塗りの色だけ
なので、SVG を自前で組み立てる方式に変えた。

| | Plotly | 自前SVG |
| --- | --- | --- |
| 組み立て | 34ms | **1.3ms** |
| 1回の転送 | 127KB | **8.9KB** |
| ちらつき | 出る | 出ない(色は CSS transition でなめらかに変わる) |
| 依存 | plotly が要る | **不要** |

凡例は自前で作る(グラデーションの帯と「※警報の強さではありません」)。
ホバーは SVG の `<title>` で出す。plotly への依存が無くなったので、
`requirements.txt` からも外した。

**表示範囲: 関東圏(茨城・栃木・群馬・埼玉・千葉・東京・神奈川・山梨)。**
全国を描くと、見たい関東が小さくて関連度が読み取れなかった。東京都は伊豆諸島・
小笠原を含むと南へ大きく伸びてしまうので、北緯 34.8 度より南のポリゴンは描かない。
GeoJSON は全国分を持っているので、範囲を変えるのは `KANTO_CODES` の書き換えだけで済む。

**GeoJSON: `assets/japan_prefectures.geojson`(111KB、47都道府県)。**
出典は**地球地図日本(国土地理院)**で、[dataofjapan/land](https://github.com/dataofjapan/land)
が GeoJSON 化したもの(元は13MB)を Douglas-Peucker で簡略化した。配布元の条件により、
**非営利利用では出典元(地球地図日本)の明記が必要**なので、地図の下と README に明記している。
実行時にネットワークから取りに行かない。

**都道府県の対応付け: 電文IDのファイル名末尾にある府県予報区コードの上位2桁。**

    .../20260919015757_0_VPWW53_130000.xml  ->  13 = 東京都

XML を読み直さないので軽い。判定済み 1,776 件で**コード不明は0件**だった。

**全国対象の電文(`010000`)は地図に塗らない。** 全般気象情報・台風情報・集約通報などが
これにあたり、判定済みのうち165件(約9%)を占める。全国を一律に塗ると地図が真っ赤になって
「自分に関係するところだけが浮かび上がる」という地図の意味が失われるため、色からは外し、
件数だけを地図の下に出している。なお `010000` は**北海道ではなく全国**である
(北海道の予報区は 011000〜017000)。取り違えるとロジックが壊れるので注意。

動作確認:

- 10問の判定 1,776 件で、東京都 0.98 / 神奈川県 0.91 / 埼玉県 0.72 / 千葉県 0.67 に対し、
  北海道・鹿児島県・沖縄県などは 0.06。首都圏だけが濃く浮かび上がる
- **神奈川県が 0.91 と高いのは、プロファイルに「高齢の両親が神奈川県内」と書いてあるため。**
  気象庁の警報マップでは出てこない、このデモならではの結果
- 50問に切り替えると地図がほぼ空になり、リプレイを進めると 9→13 都道府県へ塗られていく
  ことを確認(地図が更新されていくことの確認)

### 対象の拡張

| ID | 要件 | 状態 |
| --- | --- | --- |
| R-70 | プロファイルを複数に拡張する(ペルソナ40人、または拠点40件) | 【未実装】 |
| R-71 | 交通量データ(国土交通省の交通量API)の重ね合わせ | 【未実装】 |

動作確認の記録(2026-09-19):

- リプレイで 2026-09-18 を60倍速再生し、80秒で180件を判定(平均271ms)
- 関連度は東京都の集約通報が 0.98、沖縄・鹿児島・鳥取が 0.02〜0.03 と妥当に分かれた
- 同じ日を再生し直すと **API 呼び出し0回・0トークン**(キャッシュが効いている)
- しきい値を 0.50→0.95 に上げると、横浜(0.87)が薄くなり気象庁(0.97)だけが残った
- 画面を再読み込みしても判定済みの履歴が残ることを確認

動作確認の記録(2026-09-20):

- 同じ電文を 10問 / 30問 / 50問 で判定して **496ms / 732ms / 713ms**。
  質問数を5倍にしてもレイテンシはほとんど変わらない(R-61 のデモの裏付け)
- 画面上の質問数別の平均レイテンシは **10問 279ms(612件) / 50問 319ms(32件)**
- 50問では、プロファイルを読んだ判定になっていることを確認
  (ペット 0.04 = プロファイルにペットがいない、高齢の家族 0.80 = 横浜に両親がいる)
- `profile.yaml` の市区町村を書き換えて再読み込みすると、ヘッダーの表示が変わり、
  前提の異なる判定が混ざっている旨の警告が出ることを確認

### 画面上部の詰め方(2026-09-20)

結論バナーは画面幅いっぱいに置いていたが、右側が空いて場所を無駄にしていた。
補足指標3つと**横一列**に並べ、タイトル・プロファイル・モード・操作ボタンも1行に
まとめた。これで関連度マップがスクロールなしで見える位置に上がった。

**注意: `.block-container` の `padding-top` を詰めすぎないこと。** Streamlit の
上部ツールバーは固定表示なので、`padding-top` をその高さ(約3.75rem)より小さくすると
先頭のタイトルがツールバーの下に潜って見えなくなる。いまは `3.6rem` にしてある。

### 再生速度を100倍まで広げた(2026-09-20)

選択肢に100倍を足したが、それだけでは60倍とほとんど変わらなかった。
**電文と電文の間に空ける待ち時間に「3秒」の固定上限があったため。**
夜間など電文が何十分も空く区間では、60倍(30秒→3秒)も100倍(18秒→3秒)も
同じだけ待つことになり、速度を上げた意味がなくなっていた。

上限を速度に応じて縮めるようにした(`REPLAY_MAX_WAIT_SEC` × 60/速度)。
60倍までは3秒のまま(デモが止まって見えないための上限なので緩めない)、
100倍では1.8秒になる。20秒の再生で、再生位置が 00:49 → 01:30 まで進むようになった。

### モードのバッジが折り返して下に重なった件(2026-09-20)

開始時刻を足したことでバッジの文字数が増え、2行に折り返して下の結論バナーに
重なった。**バッジは必ず1行に収める**こと。

- 中身は「モード名・日付・速度・再生中の時刻」までにとどめる。
  開始時刻はサイドバーで見えるのでバッジには出さない
- `.jev-badge` に `white-space:nowrap` と `overflow:hidden` を付け、
  入りきらなければ省略記号にする(折り返させない)
- 包む `.jev-modebar` に `min-height` と `overflow:hidden` を付けて、
  万一伸びても下の要素に被らないようにする

あわせて、地図の横の注記が**全国**の1位(沖縄県など)を出していたのを、
**関東の中の1位**に直した。地図は関東圏なので、一覧に無い県の名前が出ると
食い違って見える。

### 関連度と警報の強さを同時に見せる(2026-09-20)

関連度の色をシアン→アンバー→レッドにしていたが、**赤や橙は「警報が強い」と
誤解される。** この地図の存在意義は「Jev が判定した自分にとっての関連度」を
見せることなので、警報の色と取り違えられるのは致命的。

2つの軸を、別の視覚要素に分けた。

| 軸 | 表し方 | 出どころ |
| --- | --- | --- |
| **関連度**(relevant) | 面の色。青 → インディゴ → 紫 → マゼンタ | **Jev の判定** |
| **事象の深刻さ**(severity) | 中央の円の大きさ。琥珀 → 赤 | 電文から読める値 |

色相が大きく離れているので混同しない。「**色が濃い＝自分に関係する／
円が大きい＝事象が深刻**」と一言で説明でき、両方そろった地域が
「自分にとって重大」だと一目で分かる。凡例も2軸に分けて並べている。

深刻さは、その地域でいちばん重い事象を見たいので**最大値**を取る
(関連度の最大値とは別の電文から来てよい。別の軸なので)。

### リプレイで画面が真っ白になった件(2026-09-20)

リプレイに切り替えたあと対象日を選ぶと、画面が真っ白になることがあった。
ブラウザのコンソールに `Bad delta path index 10 (should be between [0, 9])`。

**原因は、短い間隔の部分更新とサイドバー操作の全体再実行がぶつかること。**
`st.fragment` を6つに分けて 0.2 秒ごとに更新していたため、部分更新の差分が
「10番目の要素を書き換えて」と言っている最中に、全体再実行で要素が9個に減り、
差分の宛先が消えて描画が壊れていた。

直した内容:

- **fragment を6つ → 2つに統合**(画面の上半分と下半分)。差分の数そのものを減らす
- **fragment の中で要素の数が変わらないようにする。** モードで 1個 ↔ 3個 と
  変わっていた操作ボタンは、常に3個置いて使えないものを CSS で隠す。
  件数によって `st.info` が出たり消えたりしていた箇所も、常に同じ器にした
- **更新間隔を 1秒に。** 0.2 秒では、上に加えて
  `Cached ForwardMsg MISS`(送ったメッセージがキャッシュから先に消える)も多発した。
  0.5 秒で MISS は消え、1秒ではコンソールのエラーが出なくなった。
  あわせて `.streamlit/config.toml` に `maxCachedMessageAge = 300` を入れてある
- 間隔を変えるときは `app/streamlit_app.py` の `run_every` を直す。
  **短くするほど不安定になるので、本番前に必ず一度は通しで触って確認すること**

### 画面の不具合と直した内容(2026-09-20)

**リプレイで画面が更新されなかった。** 原因は表示側で、リプレイは正しく進んでいた。
「注目の判定」「流れる電文」「地図」「結論バナー」がいずれも**全判定結果を新しい順**に
出していたため、再生位置が朝でも最新(夜)の電文が先頭に出続けていた。進捗カウンタだけが
進み、中身が変わらないので「動いていない」ように見えた。

再生位置(`Status.replay_position`)を持たせ、リプレイ中は `updated <= 再生位置` の
判定結果だけを見せるようにした(R-59f)。あわせてモード帯に再生中の時刻を出し、
モード帯自体も2秒ごとに描き直すようにした(R-59g)。

なお、9/19 は 1,977件中 1,719件が判定済みだったため、キャッシュが効いて新しい判定が
ほとんど発生しなかったことも「変化が見えない」一因だった(これは仕様どおりの動作)。

### 警戒レベルとの関係(2026-09-20)

行動の副題に「警戒レベル○ 相当」を出している。対応は次のとおり。

| 行動の表示 | 副題 | 選択肢名(Jev に渡す値) |
| --- | --- | --- |
| いつもどおりで | 平常の行動 | 通常どおり |
| 予定の見直しを | 警戒レベル1 相当 | 予定変更を検討 |
| 早めの備えを | 警戒レベル2 相当 | 今日中に備える |
| 外出は控えて | 警戒レベル3 相当 | 外出を控える |
| 早めの避難を | 警戒レベル4 相当 | 早めの避難を検討 |

**「相当」を必ず付けること。** 警戒レベルは**地域の状況**に対して行政(レベル3〜5の
避難情報)と気象庁(レベル1〜2の防災気象情報)が出すものだが、ここで出しているのは
**この人の事情を踏まえた個人の行動**で、別物である。言い切ると避難指示などの
発令と取り違えられる。副題には「発令そのものではない」旨の注記を添えてある。

警戒レベル5(緊急安全確保)に対応する段階は、いまの5段階には無い。

出典: 気象庁「防災気象情報と警戒レベルとの対応について」
https://www.jma.go.jp/jma/kishou/know/bosai/alertlevel.html
(内閣府「避難情報に関するガイドライン」令和8年3月改定に対応)

**表示名と選択肢名は分けてある。** `core/questions.py` の criteria のキーが
Jev に渡す選択肢名で、画面の文言は `app/streamlit_app.py` の `ACTION_LABELS`。
選択肢名を変えると、保存済みの判定結果と食い違って色が付かなくなり、
揃えるには全件を判定し直すことになる。

### 画面から外した説明(2026-09-20)

見せたいもの(結論・速報欄・地図)を邪魔しないよう、次の説明を画面から外した。
内容は README と requirements.md に書いてあるので、画面で繰り返す必要はない。

- ヘッダーの「読み込み元: profile.yaml」→ 下の「Jev の性能」の行に出ている
- サイドバーの「判定結果は data/judged/ に保存され…」→ README に記載
- 地図の横の「○○県が最も高いのは、「…」の判定によるものです」
  → どの電文で最大になったかは、地図のホバーで出る

### アプリ名について(2026-09-20)

画面に出す名前は「**東京の防災電文、Jevで判定してみた**」。

- **台風はアプリ名に入れない。** 台風25号が過ぎた瞬間に古くなるため。
  このアプリは大雨でも大雪でも動く。時事性は X の投稿文の側で持たせる
  (例: 「台風25号が近づく東京で、気象庁の防災電文をぜんぶ Jev に判定させてみた」)
- ブラウザのタブは幅が狭いので「東京の防災電文 × Jev」と短くする
- **リポジトリ名 `jev-stormboard` は変えない。** 変えると URL とリモートが変わる

名前は `app/streamlit_app.py` の `APP_TITLE` / `TAB_TITLE` にまとめてある。

---

## 公開の方針

このアプリは **X(旧Twitter)で公開する**予定です。リポジトリも **public のまま**
運用します。今後プロファイルや画面を変更するときは、この節を判断基準にしてください。

### 基本の考え方

**個人情報がコミットされない仕組みにする。** 運用でどうにかするのではなく、
仕組みで防ぎます。

| ファイル | 中身 | git |
| --- | --- | --- |
| `profile.yaml` | **公開用の仮プロファイル。** 架空の内容 | コミットする |
| `profile.local.yaml` | 自分の実際の状況 | **`.gitignore` 済み。コミットされない** |
| `profile.local.example.yaml` | 書き方の例(仮の内容) | コミットする |
| `data/judged/` | 判定結果。プロファイルの指紋を含む | `.gitignore` 済み |

アプリは `profile.local.yaml` があればそちらを優先し、無ければ `profile.yaml` を
読みます。どちらを読んだかは画面の右上に小さく出ます。

### area_code を予報区コードにした理由

公開用の `profile.yaml` では、市区町村コード(7桁)ではなく
**東京都の府県予報区コード 130000**(6桁)を使っています。市区町村まで書くと
住んでいる場所が絞られるためです。

**判定の質が保てることを確認済みです**(2026-09-20)。同じ電文を、市区町村コード
(A: 都内のある区・7桁)と予報区コード(B: 130000・市区町村なし)の両方で判定しました。

| 電文 | 案 | relevant | imminent | severity | impact | action |
| --- | --- | --- | --- | --- | --- | --- |
| 東京都の警報 | A | 0.97 | 0.26 | 2.69 | 1.83 | 予定変更を検討 |
| 東京都の警報 | B | 0.97 | 0.27 | 2.73 | 2.03 | 今日中に備える |
| 千葉の台風情報 | A | 0.13 | 0.64 | 2.90 | 2.59 | 予定変更を検討 |
| 千葉の台風情報 | B | 0.14 | 0.65 | 2.89 | 2.97 | 予定変更を検討 |
| 山梨の台風情報 | A | 0.06 | 0.29 | 2.13 | 2.52 | 今日中に備える |
| 山梨の台風情報 | B | 0.06 | 0.28 | 2.19 | 2.55 | 今日中に備える |

いちばん重要な **relevant(この地域に関係する)はほぼ同一**で、関係あり/なしの
切り分けは変わりません。東京都の警報で action が1段変わっていますが、これは
`impact` が 1.83→2.03 と境界付近にあるためで、市区町村の有無による系統的な差では
ありません。

より細かい判定が欲しい場合は、`profile.local.yaml` に市区町村コードを書いてください。

### スクリーンショットと録画の注意

**画面の表示名は profile の `name` をそのまま出します。** `profile.local.yaml` を
使っている場合、実名やお住まいの市区町村が画面に出ることがあります。

- スクリーンショットや録画を公開する前に、**ヘッダーの表示名と地域を確認する**
- 判定カードのキャプションには**プロファイルの指紋**が出る(中身そのものではないが、
  同一人物かどうかは分かる)
- 公開用の画面を撮るときは、`profile.local.yaml` を一時的に別名にして
  `profile.yaml` で起動するのが確実(画面右上の「読み込み元」で確認できる)

---

## Noul の確信度の扱い

Jev の SDK が返す答えは、型によって形が違います。

| 型 | 値 | 確率 | 確信度 |
| --- | --- | --- | --- |
| Choice | `choice`(選択肢名) | `probabilities`(選択肢ごと) | `confidence` |
| Score | `score`(数値) | `probabilities`(段階ごと) | `confidence` |
| Noul | `noul`(0〜1) | 値そのものが確率 | **SDKは返さない** |

**Noul の値は「命題が真である確率」そのもの**であり、Choice / Score の `confidence` とは
意味が異なります(`confidence` は「モデルがその答えをどれだけ確信しているか」)。混同しないこと。

### 画面のしきい値スライダーについて(2026-09-20 に判断)

Noul のしきい値スライダーは、当初「何を絞るものか画面から分からない」状態でした。
**外さずに残す**ことにしています。質問数を50問に増やすと、0.5 付近の
「どちらとも言えない」回答がカード内に多く並ぶため、それを薄くして読みやすくする
用途がはっきりしたからです。あわせて次を直しました。

- ラベルを「判断がついた回答だけを濃く」に変更(何が起きるかが分かる言い方にする)
- スライダーの下に、用途を1行で添える
- ヘルプに、Noul の値と確信度の違いを書く

そのため、**画面でしきい値による絞り込みを行う際は、Noul については `0.5` からの距離
(`abs(noul - 0.5) * 2`)を確信度の代わりに用います。** 値が 0 または 1 に近いほど
「真とも偽とも判断がついている」、0.5 に近いほど「どちらとも言えない」という意味になります。

---

## 参考: 電文XMLの構造(確認済み)

保存済みの電文を読んで確認した構造です(2026-09-19 時点)。

共通部分:

- `Report/Control`: Title(電文の種類名) / DateTime / Status / EditorialOffice / PublishingOffice
- `Report/Head`: Title(見出し) / ReportDateTime(JST) / TargetDateTime / InfoType(発表・訂正・取消) /
  Serial / InfoKind / Headline/Text(要約文)

種類ごとの本文:

- **気象警報・注意報**(VPWW53 / VPWW54): `Body/Warning[@type]/Item/Area{Name, Code}` と
  `Item/Kind{Name, Status}`。type は府県予報区等 / 一次細分区域等 / 市町村等をまとめた地域等 / 市町村等の4階層
- **府県気象情報**(VPFJ50): `Body/Comment/Text` に平文

要素ごとに既定の名前空間が異なる(`jmaxml1/`、`jmaxml1/body/meteorology1/`、
`jmaxml1/informationBasis1/`)ため、パース後にタグから名前空間を落として扱います。

地域コードの階層(東京都の例):

| 区分 | 例 | コード |
| --- | --- | --- |
| 府県予報区 | 東京都 | 130000 |
| 一次細分区域 | 東京地方 | 130010 |
| 市町村等をまとめた地域 | ２３区西部 | 130011 |
| 市町村等 | 新宿区 | 1310400 |
